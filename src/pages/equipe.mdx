---
layout: ../layouts/PageLayout.astro
---

import teams from "../content/teams.json";
import Member from "../components/Member.svelte";

# L'équipe !

Chaque année, un nouveau bureau est élu pour gérer l'association. Voici les membres de l'équipe actuelle :

{teams.map((team) => (

<div class="team">
    <div class="flex-center-row">
        {team.members.map((member) => (
            <Member transition:slide {...member} />
        ))}
    </div>
</div>
))}
